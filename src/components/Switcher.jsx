import { BrowserRouter, Routes, Route } from "react-router-dom";

const Switcher = () => {
    return (
        <BrowserRouter>
            <Routes>
                <Route path="" element={''}/>
            </Routes>
        </BrowserRouter>
    )
}

export default Switcher